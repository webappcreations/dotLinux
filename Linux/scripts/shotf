#
# shot 
# using scrot + filename to save screenshots from the run comand field or of cause the console itself
# part of myLinux (this should be a variable)

[ -z "$1" ] && exit 1

mkdir ~/myLinux/shots/$1
